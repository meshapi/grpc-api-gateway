# yaml-language-server: $schema=/home/peyman/projects/meshapi/grpc-rest-gateway/internal/codegen/httpspec/Config.schema.json
gateway:
  endpoints:
    - selector: '.EchoService.Echo'
      custom:
        path: '/v1/alpha'
        method: 'REGISTER'
      disable_query_param_discovery: true
      query_params:
        - selector: 'pagination.per_page'
          name: 'per_page'
